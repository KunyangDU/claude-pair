# API Key (optional — if not set, no auth required)
# Can also use: export REMOTE_VIBING_API_KEY="xxx"
auth:
  # api_key: ""

# Server settings
server:
  port: 8787
  # default_folder: "/path/to/your/project"

# Remote access URL — how external clients reach this server
# Cloudflare Tunnel, ngrok, frp, Tailscale, or any reverse proxy
remote:
  # url: "https://your-domain.com/v1"
